<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
  {{> head}}
</head>
<body class="h-100 d-flex flex-column">
  {{!-- only show nav if hideNav is not true --}}
  {{#unless hideNav}}
    {{> nav}}
  {{/unless}}

  <main class="flex-fill">
    {{{body}}}
  </main>

  {{> footer}}
</body>
</html>
